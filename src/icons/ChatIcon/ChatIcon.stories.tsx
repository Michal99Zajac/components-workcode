import React from 'react'
import { Meta, Story } from '@storybook/react'

import ChatIcon from './ChatIcon'

export const defaultRingIcon: Story = () => <ChatIcon />

export default {
  title: 'icon/ChatIcon',
} as Meta
