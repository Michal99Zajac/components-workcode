import { Theme, ThemeComponents } from '@chakra-ui/react'

export const hStackTheme: ThemeComponents = {
  HStack: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type HStackTheme = Theme & typeof hStackTheme

export default hStackTheme
