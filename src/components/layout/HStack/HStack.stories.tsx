import React from 'react'
import { Meta, Story } from '@storybook/react'

import HStack from './HStack'

export const primary: Story = () => <HStack>hello</HStack>

export default {
  title: 'components/layout/HStack',
} as Meta
