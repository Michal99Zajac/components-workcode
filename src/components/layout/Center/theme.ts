import { Theme, ThemeComponents } from '@chakra-ui/react'

export const centerTheme: ThemeComponents = {
  Center: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type CenterTheme = Theme & typeof centerTheme

export default centerTheme
