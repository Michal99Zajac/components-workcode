import { Theme, ThemeComponents } from '@chakra-ui/react'

export const gridItemTheme: ThemeComponents = {
  GridItem: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type gridItemTheme = Theme & typeof gridItemTheme

export default gridItemTheme
