import { Theme, ThemeComponents } from '@chakra-ui/react'

export const vStackTheme: ThemeComponents = {
  VStack: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type VStackTheme = Theme & typeof vStackTheme

export default vStackTheme
