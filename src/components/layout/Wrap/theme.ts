import { Theme, ThemeComponents } from '@chakra-ui/react'

export const wrapTheme: ThemeComponents = {
  Wrap: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type WrapTheme = Theme & typeof wrapTheme

export default wrapTheme
