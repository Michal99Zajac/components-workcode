import { Theme, ThemeComponents } from '@chakra-ui/react'

export const boxTheme: ThemeComponents = {
  Box: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type BoxTheme = Theme & typeof boxTheme

export default boxTheme
