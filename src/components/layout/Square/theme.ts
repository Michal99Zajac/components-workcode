import { Theme, ThemeComponents } from '@chakra-ui/react'

export const squareTheme: ThemeComponents = {
  Square: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type SquareTheme = Theme & typeof squareTheme

export default squareTheme
