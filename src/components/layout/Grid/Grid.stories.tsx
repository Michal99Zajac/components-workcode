import React from 'react'
import { Meta, Story } from '@storybook/react'

import Grid from './Grid'

export const primary: Story = () => <Grid>hello</Grid>

export default {
  title: 'components/layout/Grid',
} as Meta
