import { Theme, ThemeComponents } from '@chakra-ui/react'

export const gridTheme: ThemeComponents = {
  Grid: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type gridTheme = Theme & typeof gridTheme

export default gridTheme
