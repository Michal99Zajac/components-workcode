import { Theme, ThemeComponents } from '@chakra-ui/react'

export const circleTheme: ThemeComponents = {
  Circle: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type CircleTheme = Theme & typeof circleTheme

export default circleTheme
