import { Theme, ThemeComponents } from '@chakra-ui/react'

export const flexTheme: ThemeComponents = {
  Flex: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type flexTheme = Theme & typeof flexTheme

export default flexTheme
