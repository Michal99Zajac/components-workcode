import { Theme, ThemeComponents } from '@chakra-ui/react'

export const stackTheme: ThemeComponents = {
  Stack: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type StackTheme = Theme & typeof stackTheme

export default stackTheme
