import { Theme, ThemeComponents } from '@chakra-ui/react'

export const pinInputTheme: ThemeComponents = {
  PinInput: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type PinInputTheme = Theme & typeof pinInputTheme

export default pinInputTheme
