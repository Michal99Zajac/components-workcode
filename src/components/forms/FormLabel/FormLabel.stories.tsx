import React from 'react'
import { Meta, Story } from '@storybook/react'

import FormLabel from './FormLabel'

export const primary: Story = () => <FormLabel>hello</FormLabel>

export default {
  title: 'components/forms/FormLabel',
} as Meta
