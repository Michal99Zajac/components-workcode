import { Theme, ThemeComponents } from '@chakra-ui/react'

export const formLabelTheme: ThemeComponents = {
  FormLabel: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type FormLabelTheme = Theme & typeof formLabelTheme

export default formLabelTheme
