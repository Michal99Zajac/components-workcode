import { Theme, ThemeComponents } from '@chakra-ui/react'

export const formErrorMessageTheme: ThemeComponents = {
  FormErrorMessage: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type FormErrorMessageTheme = Theme & typeof formErrorMessageTheme

export default formErrorMessageTheme
