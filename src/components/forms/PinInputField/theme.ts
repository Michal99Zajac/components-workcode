import { Theme, ThemeComponents } from '@chakra-ui/react'

export const pinInputFieldTheme: ThemeComponents = {
  PinInputField: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type PinInputFieldTheme = Theme & typeof pinInputFieldTheme

export default pinInputFieldTheme
