import { Theme, ThemeComponents } from '@chakra-ui/react'

export const sliderTheme: ThemeComponents = {
  Slider: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type SliderTheme = Theme & typeof sliderTheme

export default sliderTheme
