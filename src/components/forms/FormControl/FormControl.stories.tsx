import React from 'react'
import { Meta, Story } from '@storybook/react'

import { FormControl } from './FormControl'

export const primary: Story = () => <FormControl>hello</FormControl>

export default {
  title: 'components/forms/FormControl',
} as Meta
