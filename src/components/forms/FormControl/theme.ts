import { Theme, ThemeComponents } from '@chakra-ui/react'

export const formControlTheme: ThemeComponents = {
  FormControl: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type FormControlTheme = Theme & typeof formControlTheme

export default formControlTheme
