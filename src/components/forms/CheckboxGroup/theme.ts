import { Theme, ThemeComponents } from '@chakra-ui/react'

export const checkboxGroupTheme: ThemeComponents = {
  CheckboxGroup: {
    parts: [],
    sizes: {},
    baseStyle: {},
    variants: {},
    defaultProps: {},
  },
}

export type CheckboxGroupTheme = Theme & typeof checkboxGroupTheme

export default checkboxGroupTheme
