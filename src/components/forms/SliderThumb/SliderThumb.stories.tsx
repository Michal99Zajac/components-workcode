import React from 'react'
import { Meta, Story } from '@storybook/react'

import SliderThumb from './SliderThumb'

export const primary: Story = () => <SliderThumb>hello</SliderThumb>

export default {
  title: 'components/forms/SliderThumb',
} as Meta
