import { ThemeComponents, Theme } from '@chakra-ui/react'

export const iconButtonTheme: ThemeComponents = {
  IconButton: {
    baseStyle: {},
    variants: {},
    sizes: {},
    defaultProps: {},
  },
}

export type IconButtonTheme = typeof iconButtonTheme & Theme

export default iconButtonTheme
